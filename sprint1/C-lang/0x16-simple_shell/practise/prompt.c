#include "shell.h"

/**
 * prompt - repeatedly prompt user for input
 * @en: environmental variables
 *
 * Return: zero(0)
 */

int prompt(char **en)
{
	list_t *env;
	size_t i = 0, n = 0;
	int command_line_no = 0, exit_stat = 0;
	char *command, *n_command, **token;


}
